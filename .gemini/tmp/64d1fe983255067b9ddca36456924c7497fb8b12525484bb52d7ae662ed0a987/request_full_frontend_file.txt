Thank you for pointing out the error. `ReferenceError: isNewProfile is not defined` indicates that the variable `isNewProfile` is being used before it's declared or in a scope where it's not accessible.

This is unexpected, as I previously instructed to replace the `useState` based `isNewProfile` with a `useMemo` derived `isNewProfile` and ensure all its usages point to this derived variable.

This error strongly suggests that:
1.  **Your frontend development server is not running the latest code.** The changes might not have been picked up.
2.  **There might be a discrepancy between the file I edited and your local file.**

To debug this effectively, I need to see the exact state of your `ProfileCVForm.tsx` file.

Please provide the **entire content** of your `C:\Users\Administrator\Documents\IBE 160 Programmering med KI\CVAI Turbo\SG-Oslo-Best\frontend\components\ProfileCVForm.tsx` file.

Also, please **ensure you have restarted your frontend development server** (`npm run dev` or `pnpm run dev`) after my last set of changes.

Once I have the full file, I can verify the declarations and usages of `isNewProfile` and identify where the `ReferenceError` is coming from.